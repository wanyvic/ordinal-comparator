# Ordinal Comparator Environment Configuration
# Primary indexer endpoint (reference)
PRIMARY_ENDPOINT=https://primary-indexer.com
# Secondary indexer endpoint (to verify)
SECONDARY_ENDPOINT=https://secondary-indexer.com
# Protocol type (ORDINAL or BRC20)
PROTOCOL=ORDINAL
# Blockchain (BITCOIN or FRACTAL)
CHAIN=BITCOIN
# Number of concurrent worker threads
THREADS=100
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
# Optional: Starting block height
START_BLOCK=
# Optional: Ending block height
END_BLOCK=
# Application progress interval
PROGRESS_INTERVAL=